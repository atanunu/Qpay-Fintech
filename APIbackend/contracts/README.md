# Generated API contracts

Run go run ./cmd/contracts from APIbackend to export openapi.json and paths.ts from the actual route registry. The running service serves /openapi.json. Build outputs are generated, not independently edited. Review [client integration](../docs/CLIENT-INTEGRATION.md) and the endpoint capability limits before generating client screens.
