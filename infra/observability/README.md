# Observability workspace
Define intent/outbox/callback age, reconciliation breaks, provider float, error rate, latency and queue lease metrics with owners and response actions. Separate business/financial outcomes from HTTP success. No high-cardinality customer IDs or sensitive payloads in metrics.
