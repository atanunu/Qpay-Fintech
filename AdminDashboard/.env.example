# Browser configuration only. No secrets or provider keys.
VITE_ADMIN_MODE=api
VITE_API_BASE_URL=https://api.example.invalid
VITE_ALLOW_LOCAL_HTTP=false
# APIbackend must list this dashboard's exact origin in ADMIN_ORIGINS.
