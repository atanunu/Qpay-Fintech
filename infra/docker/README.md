# Docker workspace
Add local/test container definitions only after real application manifests and health endpoints exist. Use synthetic databases and private service networking. Never embed keys, production defaults, destructive reset commands or publicly exposed financial databases.
