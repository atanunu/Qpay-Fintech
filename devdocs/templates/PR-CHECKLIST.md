# Pull request evidence
Link stable feature IDs, approved scope and dependencies. Explain code/schema/API/security changes and rollback implications. List exact executed commands, source commit, environment, results and skips. Link source, test and visual artifacts. Identify provider/live acceptance that remains outstanding.

Update canonical service README and derived root/JSON; run documentation and affected application checks. Update changed real screenshots or provide reviewed no-visual-impact rationale. Confirm no secrets, real customer records or production-auth bypasses. Verify required CI and remote commit before reporting delivery. A template alone does not enforce these checks.
