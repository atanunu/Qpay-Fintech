# Runtime screenshot gallery

The runnable console now has a review-only capture suite. Capture is pending ordinary browser CI because local Chromium HTTP navigation is blocked by environment policy. The architecture diagram in the README is not a runtime screenshot. Screenshots will be committed with exact source revision and hashes after reviewing the CI artifact. No authentication tokens or real customer evidence may be included.
