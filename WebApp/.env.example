# Browser-visible values only. No API secrets, Novu keys or provider credentials.
VITE_API_MODE=api
# Keep the same hostname as the web origin for local SameSite cookies.
VITE_API_BASE_URL=http://localhost:8080
VITE_ENABLE_REVIEW_TOOLS=true
# The app deliberately blocks money submission to production in this test release.
